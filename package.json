{
  "name": "app-letstalk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node ./server/dist/server.js",
    "heroku-postbuild": "cd server && yarn install && cd ../client && yarn install"
  },
  "engines": {
    "node": "10.15.3",
    "yarn": "1.19.1",
    "parcel-bundler": "^1.12.4"
  }
}
